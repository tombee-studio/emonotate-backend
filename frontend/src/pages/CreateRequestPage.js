import React from "react";
import { 
    Box, 
    FormControl,
    TextField
} from "@material-ui/core";

const CreateRequestPage = () => {
    return (
    <Box m={2}>
        <FormControl>
            <TextField />
        </FormControl>
    </Box>);
};

export default CreateRequestPage;
